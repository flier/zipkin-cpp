sudo: required

dist: trusty

services:
    - docker

os:
    - linux
    - osx

addons:
    apt:
        packages:
            - automake
            - bison
            - flex
            - build-essential
            - clang
            - cmake
            - g++
            - libboost-dev
            - libboost-filesystem-dev
            - libboost-program-options-dev
            - libboost-system-dev
            - libboost-regex-dev
            - libboost-test-dev
            - libevent-dev
            - libssl-dev
            - libtool
            - make
            - pkg-config
            - curl
            - tree

language: cpp

compiler:
    - clang
    - gcc

python:
    - "2.7"

before_script:
    - .travis/install-dependencies.sh

script:
    - cmake .
    - make
    - make test

after_failure:
    - echo `pwd`
    - tree -h